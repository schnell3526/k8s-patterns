apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: cilium-hubble-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
        role: backend
    spec:
      containers:
        - name: nginx
          image: nginx:1.29
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: cilium-hubble-demo
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 80
