apiVersion: v1
kind: Pod
metadata:
  name: demo-workload
  namespace: tetragon-audit-demo
  labels:
    app: demo-workload
spec:
  containers:
    - name: netshoot
      image: nicolaka/netshoot:latest
      command: ["sleep", "infinity"]
